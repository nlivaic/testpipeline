mode: Mainline
major-version-bump-message: 'breaking\:'
minor-version-bump-message: 'feature\:'
patch-version-bump-message: 'fix\:'
branches:
  main:
    regex: ^master$|^main$
  feature:
    regex: ^feature[/-]
    tag: useBranchName
  hotfix:
    regex: ^fix[/-]
    tag: useBranchName
